# Desktop design records

These files are design records, not product UI or release evidence. Their repositories,
goals, files, counts, diffs, messages, and results are examples. Do not use them as
application fallback data.

A shipped concept must use typed application state or be absent. Use explicit empty,
unavailable, loading, and error states. Do not simulate files, diagnostics, activity,
evidence, progress, or success.
